using System;
class ThreeIntegerSum
{
    static void Main()
    {
        // Define integer variables and read them from the console.

        int numOne = int.Parse(Console.ReadLine());
        int numTwo = int.Parse(Console.ReadLine());
        int numThree = int.Parse(Console.ReadLine());

        //Define the "sum" variable that will keep the sum of numOne, numTwo and numThree and print it on the console.

        int sum;
        Console.WriteLine(sum=numOne+numTwo+numThree);
    }
}
